import { v4 as uuid} from "uuid";

export abstract class <%= props.Module %>Fields {
  id?: string

  createId(id?: string) {
    if (!id) {
      return uuid();
    }
      
    if (typeof id !== "string") {
      throw new TypeError(`Expected id to be string (uuid)`)
    }

    return id
  }
}
